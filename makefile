# remove docker
remove-docker:
	rm -rf ./docker
remove-k8s:
	rm -rf ./k8s
remove-storybook:
	rm -rf ./storybook
# remove stripe
# remove next-auth
# remove project.inlang
# remove tailwind css
# remove pre-commits
# remove playwright
# remove jest